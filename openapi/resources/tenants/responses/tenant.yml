description: A tenant, in JSON format.

content:
  application/json:
    schema:
      $ref: ../models/tenant.yml
    examples:
      secret:
        $ref: ../examples.yml#/tenant

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
