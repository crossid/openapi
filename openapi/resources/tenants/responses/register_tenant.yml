description: A response of tenant registration, in JSON format.

content:
  application/json:
    schema:
      $ref: ../models/tenant_registration_response.yml
    examples:
      tenant:
        $ref: ../examples.yml#/tenant_registration_response

headers:
  ratelimit-limit:
    $ref: ../../../shared/headers.yml#/ratelimit-limit
  ratelimit-remaining:
    $ref: ../../../shared/headers.yml#/ratelimit-remaining
  ratelimit-reset:
    $ref: ../../../shared/headers.yml#/ratelimit-reset
