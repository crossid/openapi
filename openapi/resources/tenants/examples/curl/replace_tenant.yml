lang: cURL
source: |-
  curl -X PUT \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $CID_TOKEN" \
    -d '{{"displayName": "ACME", "active": "true", ...}' \
    "https://{tenant}.crossid.io/api/v1/tenants/acme"
