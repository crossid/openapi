operationId: activateTenant
summary: Activate a tenant registration.
description: Activate a tenant by an activation token, once registration completed.
tags:
  - Global Tenants
parameters:
  - $ref: parameters.yml#/activation
responses:
  '200':
    $ref: responses/tenant.yml
  default:
    $ref: ../../shared/responses/default_error.yml
x-codeSamples:
  - $ref: 'examples/curl/activate_tenant.yml'
