description: Health check response model
type: object
properties:
  status:
    type: string
    enum: ['OK', 'Service Unavailable']
  errors:
    type: object
    additionalProperties:
      type: string
required:
  - status
